name: weatherfy_api
description: Weatherfy backend server — Dart/Shelf
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  http: ^1.2.0
  dotenv: ^4.1.0

dev_dependencies:
  lints: ^3.0.0